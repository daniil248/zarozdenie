# 📘 ИНСТРУКЦИЯ: Где подставлять ссылки на политику конфиденциальности

## 🔴 ВАЖНО: Ссылки на политику конфиденциальности и соглашение

### Текущая ситуация:
Сейчас в коде используются файлы:
- `privacy-policy.html` - для политики конфиденциальности
- `terms-of-use.html` - для соглашения об использовании

### ⚠️ Проблема:
Эти файлы сейчас только на русском языке. Вам нужно создать версии для всех языков!

## 📝 Что нужно сделать:

### 1. Создать файлы для каждого языка:

**Русский язык (уже есть):**
- `privacy-policy.html` ✅
- `terms-of-use.html` ✅

**Английский язык (нужно создать):**
- `privacy-policy-en.html` ❌
- `terms-of-use-en.html` ❌

**Китайский язык (нужно создать):**
- `privacy-policy-cn.html` ❌
- `terms-of-use-cn.html` ❌

### 2. Где находятся ссылки в коде:

#### В файле `index.html`:

**Строка 209** - в форме обратной связи:
```html
<a href="privacy-policy.html" class="privacy-link">Политика конфидициальности</a>
```

**Строка 252** - в футере:
```html
<a href="terms-of-use.html">Соглашение об использовании</a> | 
<a href="privacy-policy.html">Политика конфиденциальности</a>
```

### 3. Как сделать ссылки мультиязычными:

#### Вариант 1: Автоматическое переключение (рекомендуется)

Обновите `script.js`, добавив функцию для переключения ссылок:

```javascript
// В функции translatePage добавьте:
function translatePage(lang) {
    // ... существующий код ...
    
    // Обновление ссылок на политику
    const privacyLinks = document.querySelectorAll('.privacy-link, .footer-link[href*="privacy"]');
    privacyLinks.forEach(link => {
        if (lang === 'en') {
            link.href = 'privacy-policy-en.html';
        } else if (lang === 'cn') {
            link.href = 'privacy-policy-cn.html';
        } else {
            link.href = 'privacy-policy.html';
        }
    });
    
    const termsLinks = document.querySelectorAll('.footer-link[href*="terms"]');
    termsLinks.forEach(link => {
        if (lang === 'en') {
            link.href = 'terms-of-use-en.html';
        } else if (lang === 'cn') {
            link.href = 'terms-of-use-cn.html';
        } else {
            link.href = 'terms-of-use.html';
        }
    });
}
```

#### Вариант 2: Ручное обновление в HTML

Добавьте атрибуты `data-translate-href`:

```html
<a href="privacy-policy.html" 
   data-translate-href-ru="privacy-policy.html"
   data-translate-href-en="privacy-policy-en.html"
   data-translate-href-cn="privacy-policy-cn.html"
   class="privacy-link">Политика конфидициальности</a>
```

И в `script.js` добавьте обработку:
```javascript
document.querySelectorAll('[data-translate-href]').forEach(link => {
    const hrefKey = `data-translate-href-${lang}`;
    if (link.hasAttribute(hrefKey)) {
        link.href = link.getAttribute(hrefKey);
    }
});
```

## 🎯 Быстрое решение (если нет времени на мультиязычность):

### Временно используйте один файл для всех языков:

1. Создайте `privacy-policy.html` с переключателем языков внутри
2. То же самое для `terms-of-use.html`
3. Добавьте в эти файлы переключатель языков (скопируйте из `index.html`)

## 📋 Чек-лист:

- [ ] Создан `privacy-policy-en.html` (английский)
- [ ] Создан `privacy-policy-cn.html` (китайский)
- [ ] Создан `terms-of-use-en.html` (английский)
- [ ] Создан `terms-of-use-cn.html` (китайский)
- [ ] Обновлен `script.js` для автоматического переключения ссылок
- [ ] Проверено, что ссылки работают на всех языках

## 💡 Рекомендации:

1. **Используйте шаблоны**: Скопируйте структуру из `privacy-policy.html` и переведите контент
2. **Проверьте юридическую корректность**: Убедитесь, что тексты соответствуют законодательству
3. **Добавьте дату обновления**: В каждом файле укажите актуальную дату
4. **Тестируйте**: Проверьте работу ссылок при переключении языков

## 🔗 Где еще могут быть ссылки:

Проверьте также:
- Email-рассылки (если есть)
- PDF-документы
- Другие страницы сайта (если будут добавлены)

---

**Важно**: После создания всех файлов обязательно протестируйте переключение языков и проверьте, что ссылки открывают правильные страницы!

